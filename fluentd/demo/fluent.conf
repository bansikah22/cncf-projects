# fluentd.conf

# Input: Read logs from a file
<source>
  @type tail
  path /logs/app.log
  pos_file /logs/app.log.pos
  tag app.log
  <parse>
    @type json
  </parse>
</source>

# Output: Print logs to the console
<match app.log>
  @type stdout
</match>
