apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: my-app-certificate
  namespace: default
spec:
  secretName: my-app-tls
  dnsNames:
  - my-app.com
  issuerRef:
    name: self-signed-issuer
    kind: ClusterIssuer
